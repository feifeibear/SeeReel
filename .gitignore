# Dependencies & build
node_modules
dist

# Secrets — never commit real keys (only .env.example with empty placeholders)
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets/

# Local runtime state — cinema-store may embed TOS presigned URLs (AK id + signatures)
data/

# Local agent runtime locks/state
.claude/

# Generated by scripts/install_agent_skill.mjs from skills/ (single source of truth)
.cursor/skills/

# Local agent runtime locks
.claude/

.DS_Store
