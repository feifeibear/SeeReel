(reelyai_backend) {
	encode zstd gzip
	log {
		output stdout
		format json
	}

	@private path /metrics /api/diagnostics
	handle @private {
		respond "not found" 404
	}

	reverse_proxy reelyai:5173
}

{$CADDY_SITE_ADDRESS} {
	{$CADDY_TLS_DIRECTIVE}
	import reelyai_backend
}

{$CADDY_HTTP_SITE_ADDRESS::80} {
	import reelyai_backend
}
